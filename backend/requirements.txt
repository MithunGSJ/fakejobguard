# backend/requirements.txt
# Core API framework
fastapi
uvicorn[standard]
python-multipart
pydantic

# HTTP & networking
requests
httpx
python-whois

# Data & ML
pandas
numpy
scikit-learn
imbalanced-learn
joblib
shap

# NLP & Vision (optional — install separately for BERT/CLIP)
# torch
# transformers
# Pillow is always needed (for image handling fallback)
Pillow

# Utilities
python-dotenv

# Visualization (for ML notebooks only)
matplotlib
seaborn
